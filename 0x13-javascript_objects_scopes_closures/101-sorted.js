#!/usr/bin/node

const dict = require('./101-data').dict;
const newDict = {};
const userId = Object.entries(dict);
const occurrence = dict[userId];

if (!newDict[occurrence] {
  newDict[occurrence] = [];
}
newDict[occurrence].push[userId]

